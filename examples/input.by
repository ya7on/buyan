import std.io;
import std.math;
import std.cfg;

module input_data;

def main( -- )
    "What is your name: "
    std.io.print
    std.io.flush
    std.io.input

    "Hello, "
    std.io.print
    std.io.print

    "How old are you: "
    std.io.print
    std.io.flush
    std.io.input

    "67\n" std.math.eq
    | -- string | { "sixseven" }
    | -- string | { "not sixseven" }
    std.cfg.if

    std.io.println
end
